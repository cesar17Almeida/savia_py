# Savia (Python) — dev dependencies
# Instala con: pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.10

# --- Build / packaging para OTA ------------------------------
# Compilación a binario nativo para distribución a estaciones en
# campo. NO compilar en el Zero 2W — usar Pi 4 o runner ARM64.
nuitka>=2.4
